n=int(input())
def pal(x,re):
    if x==0:
        return re
    re=re*10(x%10)
    return 




















'''def is_palindrome(num):
  if num // 10 == 0:
    return True
  return num % 10 == num // 10** (len(str(num)) - 1) and is_palindrome(num // 10)

# Example usage
number = 121
if is_palindrome(number):
  print(f"{number} is a palindrome")
else:
  print(f"{number} is not a palindrome")'''
